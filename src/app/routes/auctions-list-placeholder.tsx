// Placeholder until Task 3 adds `pages/auctions-list` and the route in
// `auctions-route.tsx` is swapped for
// `lazyRouteComponent(() => import('@/pages/auctions-list'))`.
export function AuctionsListPlaceholder() {
  return <div>Auctions list</div>
}
