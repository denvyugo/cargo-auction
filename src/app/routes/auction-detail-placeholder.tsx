// Placeholder until Task 5 adds `pages/auction-detail` and the route in
// `auction-detail-route.tsx` is swapped for
// `lazyRouteComponent(() => import('@/pages/auction-detail'))`.
export function AuctionDetailPlaceholder() {
  return <div>Auction detail</div>
}
